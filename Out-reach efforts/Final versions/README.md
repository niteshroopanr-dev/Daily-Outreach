This folder is for Nitesh only. The nightly run never writes here.

When a run's brief is edited into its final form, save it here with the same date stamped file name as the original in the parent folder, so the two can be compared later.
