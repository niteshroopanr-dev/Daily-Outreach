This folder is for Nitesh only.

The nightly run never writes here. When Nitesh edits a nightly brief into
its final, sent ready form, he saves it here using the same date stamped
file name as the original in the parent folder. The original run output
and the final edited version then sit side by side, so recurring edits
can be reviewed and fed back into the nightly outreach instructions as
new rules.
