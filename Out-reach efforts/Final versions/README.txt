This folder holds Nitesh's edited, final versions of nightly outreach decks,
saved here with the same date stamped file name as the original run output,
so the two can be compared later. The nightly run never writes into this
folder itself.
