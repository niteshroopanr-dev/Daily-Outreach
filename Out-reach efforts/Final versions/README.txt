This folder is for Nitesh only. The nightly run never writes here.

When a deck from Out-reach efforts is edited into its final, sent ready form,
save it here with the same date stamped file name as the original. The
original run output and the final version then sit side by side for later
comparison, so recurring edits can be fed back into the nightly instruction
as new rules.
